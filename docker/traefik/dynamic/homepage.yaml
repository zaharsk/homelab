http:
  services:
    homepage:
      loadBalancer:
        servers:
          - url: http://100.76.138.6:3000
  
  routers:
    homepage-private:
      entryPoints:
        - https-ep
      rule: Host(`homepage.{{ env "DOMAIN_0" }}`)
      service: homepage
      middlewares:
        - securityHeaders
        - privateAccess
