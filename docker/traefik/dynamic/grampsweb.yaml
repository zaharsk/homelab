http:
  services:
    grafana:
      loadBalancer:
        servers:
          - url: http://100.76.228.137:5000
  
  routers:
    grafana-private:
      entryPoints:
        - https-ep
      rule: Host(`tree.{{ env "DOMAIN_1" }}`)
      service: grafana
      middlewares:
        - securityHeaders
        - privateAccess
